org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# 工程目录路径含中文，AGP 默认会拒绝构建，这里关掉该检查
android.overridePathCheck=true
# flutter_inappwebview 仍在用已被 AGP 9 默认禁用的 proguard-android.txt，
# 暂时允许旧写法，等插件适配后再删掉这行
android.r8.proguardAndroidTxt.disallowed=false
# pub 缓存在 C 盘、工程在 D 盘，Kotlin 增量编译算源文件相对路径时跨盘符会抛
# IllegalArgumentException，缓存写不进去，整个编译就失败了，先关掉。这个工程
# Kotlin 代码很少，少了增量编译不影响构建速度。
kotlin.incremental=false
